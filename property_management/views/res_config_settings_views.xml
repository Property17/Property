<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form_property" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.property.management</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="40"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Property Management" string="Property Management"
                    name="property_management"
                    groups="property_management.group_property_manager,property_management.group_property_owner">
                    <block title="Deposit">
                        <setting string="Deposit Receivable Account"
                            help="Debit account on deposit receive invoices (insurance invoice only). Credit side uses the tenant insurance account on the invoice line.">
                            <field name="property_deposit_receivable_account_id"
                                options="{'no_create': True}"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_property_management_configuration" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module': 'property_management', 'bin_size': False}</field>
    </record>

    <menuitem id="menu_property_management_settings"
        name="Settings"
        parent="menu_configuration_main"
        action="action_property_management_configuration"
        sequence="1"
        groups="property_management.group_property_manager,property_management.group_property_owner"/>

</odoo>
